// export function writeToNote(... all the params, fileWriter: FileWriter (can be ObsidianFileWriter or MockFileWriter))

import { IFile } from "src/interfaces/IFile";

export function writeToNote(file: IFile) {
	// IFile can be ObsidianFile or TestFile
}
